export interface Contact {
  id: string;
  name: string;
  gender: string;
  teamMember: string;
  age: string;
  remarks: string;
  occupation: string;
  status: string;
  updatedAt: string;
  highPriority: boolean;
  socialMedia: string;
}
